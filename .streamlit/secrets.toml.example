# Template for local Streamlit secrets — copy to .streamlit/secrets.toml
# (gitignored, never commit the real file) if you want to test the frontend
# locally against the deployed backend instead of a local one.
#
# On Streamlit Community Cloud, these same two keys are set via the app's
# Settings -> Secrets panel in the web UI, not this file.

BACKEND_URL = "https://dalalstreet-agent-backend.nicemushroom-fed075ab.centralindia.azurecontainerapps.io"
BACKEND_API_KEY = "paste-the-real-key-here-never-commit-it"
